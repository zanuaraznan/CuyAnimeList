const HeaderMenu = ({ title }) => {
   return (
      <div className="w-full grid place-items-center my-10">
         <div className="text-2xl text-zinc-300">{title}</div>
      </div>
   );
};

export default HeaderMenu;
